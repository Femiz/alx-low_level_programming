#include <stdio.h>

/**
 * main - print base16
 * Return: 0 if successful
 */

int main(void)
{
	int n = 0;

	char i = "f";

	while (n < 10)
	{	
		putchar(n + '0');
	}
	while (i <= f)
	{
		putchar(i)
	}
	putchar('\n')
	return (0);
}
